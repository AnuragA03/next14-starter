//This page is created to be visible when our network is slow/ or data fetching is taking place that would take time.

const Loading = () => {
    return (
        <div>Loading...</div>
    )
}

export default Loading